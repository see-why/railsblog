<section class="form-section">
    <h1>Create new comment</h1>
<%= form_with model: @comment, url: user_post_comments_url(post_id: @post.id) ,class: 'post-form' do |f|%>
    <%= f.label :text %>
    <br>
    <%= f.text_area :text %>
    <br>
    <%= f.submit "Submit" %>
<% end %>
</section>